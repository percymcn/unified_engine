---
phase: 12-critical-fixes-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-next/src/components/sidebar.tsx
  - ui-next/src/app/dashboard/page.tsx
  - ui-next/src/app/(dashboard)/page.tsx
  - ui-next/src/app/api/auth/login/route.ts
  - ui-next/src/app/api/auth/me/route.ts
  - ui-next/src/app/api/api-keys/route.ts
  - ui-next/src/app/api/api-keys/[id]/route.ts
  - ui-next/src/app/api/brokers/health/route.ts
  - ui-next/src/app/dashboard/settings/webhooks/page.tsx
  - ui-next/.env.local
  - ui-next/.env.example
autonomous: true
must_haves:
  truths:
    - "All UI text shows 'Tradeflow' instead of 'Unified Engine'"
    - "No hardcoded localhost URLs remain in source code"
    - "Webhook URLs display with public domain"
  artifacts:
    - path: "ui-next/src/components/sidebar.tsx"
      provides: "Branding in sidebar"
      contains: "Tradeflow"
    - path: "ui-next/.env.example"
      provides: "Environment variable template"
      contains: "NEXT_PUBLIC_BACKEND_URL"
  key_links:
    - from: "ui-next/src/app/api/*.ts"
      to: "process.env.BACKEND_URL"
      via: "environment variable lookup"
      pattern: "BACKEND_URL.*8765"
---

<objective>
Rebrand UI from "Unified Engine" to "Tradeflow" and standardize environment variable usage.

Purpose: Establish consistent branding and eliminate hardcoded localhost URLs that break production deployment.
Output: All UI displays "Tradeflow" and all API routes use consistent env vars with correct default port (8765).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ui-next/src/components/sidebar.tsx
@ui-next/src/app/dashboard/page.tsx
@ui-next/src/app/api/auth/login/route.ts
@ui-next/.env.local
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand UI text to Tradeflow</name>
  <files>ui-next/src/components/sidebar.tsx, ui-next/src/app/dashboard/page.tsx, ui-next/src/app/(dashboard)/page.tsx</files>
  <action>
    Replace all "Unified Engine" text with "Tradeflow":
    1. sidebar.tsx: Change app name in header from "Unified Engine" to "Tradeflow"
    2. sidebar.tsx: Update footer version text from "Trading Signal Router v1.0" to "Tradeflow v1.1"
    3. dashboard/page.tsx: Change welcome message from "Welcome to Unified Engine" to "Welcome to Tradeflow"
    4. (dashboard)/page.tsx: Same change if different file

    Keep the Activity icon or replace with a more fitting icon if desired.
  </action>
  <verify>grep -r "Unified Engine" ui-next/src/ returns no results</verify>
  <done>All UI displays "Tradeflow" branding</done>
</task>

<task type="auto">
  <name>Task 2: Standardize BACKEND_URL defaults to port 8765</name>
  <files>ui-next/src/app/api/auth/login/route.ts, ui-next/src/app/api/auth/me/route.ts, ui-next/src/app/api/api-keys/route.ts, ui-next/src/app/api/api-keys/[id]/route.ts, ui-next/src/app/api/brokers/health/route.ts</files>
  <action>
    Fix inconsistent default port values. Several files incorrectly default to port 8000 instead of 8765:

    Change in each file:
    - FROM: const BACKEND_URL = process.env.BACKEND_URL || 'http://localhost:8000';
    - TO:   const BACKEND_URL = process.env.BACKEND_URL || 'http://localhost:8765';

    Also fix brokers/health/route.ts which uses NEXT_PUBLIC_BACKEND_URL:
    - FROM: process.env.NEXT_PUBLIC_BACKEND_URL || 'http://localhost:8000'
    - TO:   process.env.BACKEND_URL || 'http://localhost:8765'

    Use BACKEND_URL (server-side) not NEXT_PUBLIC_BACKEND_URL for API routes.
  </action>
  <verify>grep -r "localhost:8000" ui-next/src/app/api/ returns no results</verify>
  <done>All API routes default to consistent port 8765</done>
</task>

<task type="auto">
  <name>Task 3: Configure webhook URL to use public domain</name>
  <files>ui-next/src/app/dashboard/settings/webhooks/page.tsx, ui-next/.env.local, ui-next/.env.example</files>
  <action>
    The webhooks page uses NEXT_PUBLIC_BACKEND_URL for displaying webhook URLs to users.
    This is correct behavior - it needs a client-side env var.

    1. Update .env.local to include the public-facing URL:
       BACKEND_URL=http://localhost:8765
       NEXT_PUBLIC_BACKEND_URL=http://localhost:8765
       NEXT_PUBLIC_WEBHOOK_BASE_URL=https://api.tradeflow.fluxeo.net

    2. Update .env.example with all three variables documented:
       # Backend URL (server-side API calls)
       BACKEND_URL=http://localhost:8765
       # Backend URL (client-side, for WebSocket)
       NEXT_PUBLIC_BACKEND_URL=http://localhost:8765
       # Public webhook URL (displayed to users)
       NEXT_PUBLIC_WEBHOOK_BASE_URL=https://api.tradeflow.fluxeo.net

    3. Update webhooks/page.tsx to use NEXT_PUBLIC_WEBHOOK_BASE_URL for the user-facing URLs:
       - Change the baseUrl state initialization to use NEXT_PUBLIC_WEBHOOK_BASE_URL
       - Fall back to NEXT_PUBLIC_BACKEND_URL if not set

       const backendUrl = process.env.NEXT_PUBLIC_WEBHOOK_BASE_URL ||
                          process.env.NEXT_PUBLIC_BACKEND_URL ||
                          'http://localhost:8765';
  </action>
  <verify>cat ui-next/.env.example shows NEXT_PUBLIC_WEBHOOK_BASE_URL documented</verify>
  <done>Webhook URLs display public domain when configured</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -r "Unified Engine" ui-next/src/ returns no results
- [ ] grep -r "localhost:8000" ui-next/src/app/api/ returns no results
- [ ] npm run build in ui-next/ succeeds without errors
- [ ] .env.example documents all required environment variables
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- UI displays "Tradeflow" branding consistently
- All API routes use correct default port (8765)
- Webhook URL configuration documented for production
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-infrastructure/12-01-SUMMARY.md`
</output>
