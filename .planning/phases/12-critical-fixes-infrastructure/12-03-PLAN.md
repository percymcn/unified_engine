---
phase: 12-critical-fixes-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-next/src/components/sidebar.tsx
  - ui-next/src/components/header.tsx
  - ui-next/src/app/dashboard/layout.tsx
autonomous: true
must_haves:
  truths:
    - "Desktop sidebar links are clickable and navigate to correct pages"
    - "Active page is visually highlighted in sidebar"
    - "Mobile hamburger menu opens and shows navigation"
    - "Mobile navigation links work correctly"
  artifacts:
    - path: "ui-next/src/components/sidebar.tsx"
      provides: "Navigation sidebar with working links"
      min_lines: 80
    - path: "ui-next/src/components/header.tsx"
      provides: "Header with mobile menu"
  key_links:
    - from: "ui-next/src/components/sidebar.tsx"
      to: "next/link"
      via: "Link components"
      pattern: "<Link.*href="
    - from: "ui-next/src/components/header.tsx"
      to: "ui-next/src/components/ui/sheet.tsx"
      via: "Sheet component for mobile menu"
      pattern: "<Sheet"
---

<objective>
Fix desktop sidebar navigation clickability and ensure mobile menu works correctly.

Purpose: Desktop sidebar navigation links are reportedly not clickable. Mobile menu should work as fallback. Active page needs clear visual indication.
Output: All navigation links clickable on all screen sizes; current page clearly highlighted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ui-next/src/components/sidebar.tsx
@ui-next/src/components/header.tsx
@ui-next/src/app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix desktop sidebar link clickability</name>
  <files>ui-next/src/components/sidebar.tsx, ui-next/src/app/dashboard/layout.tsx</files>
  <action>
    Investigate and fix why desktop sidebar links aren't clickable.

    Potential issues to check and fix:
    1. **Z-index/stacking context**: Another element may be overlaying the sidebar
       - Check if main content area overlaps sidebar
       - Ensure sidebar has appropriate z-index if needed

    2. **Pointer events**: Check if any parent has pointer-events: none
       - Inspect CSS classes on sidebar and its parents

    3. **Link structure**: Verify Link components are properly structured
       - Links should have proper href attributes
       - No nested interactive elements

    4. **Layout issues**: Check flex layout in dashboard/layout.tsx
       - Sidebar: className="hidden md:flex" - this is correct
       - Ensure the main content div doesn't expand over sidebar

    Read the current implementation and identify the issue:

    In dashboard/layout.tsx, the layout structure is:
    ```
    <div className="flex h-screen">
      <Sidebar className="hidden md:flex" />
      <div className="flex flex-1 flex-col overflow-hidden">
        <Header />
        <main>...</main>
      </div>
    </div>
    ```

    In sidebar.tsx, links use:
    ```
    <Link href={fullHref} className="...">
    ```

    Likely fixes:
    1. Add relative positioning to sidebar if needed: className="relative z-10 ..."
    2. Ensure sidebar has proper width constraint: w-64 is set
    3. Check if any CSS is applying pointer-events: none

    Test by clicking sidebar links in browser dev tools - check for any element covering them.

    If the issue is the sidebar being hidden behind content:
    - Add z-index to sidebar: className="hidden md:flex z-10 ..."
    - Or add relative/z-index to layout wrapper

    If links themselves aren't interactive:
    - Ensure Link has proper passHref or is correctly wrapping content
    - Check for any CSS that might be blocking clicks
  </action>
  <verify>Sidebar links are clickable on desktop (md+ breakpoint) and navigate correctly</verify>
  <done>Desktop sidebar navigation links are clickable and functional</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix active state indicators</name>
  <files>ui-next/src/components/sidebar.tsx</files>
  <action>
    Review and fix active state highlighting in sidebar navigation.

    Current implementation checks:
    ```typescript
    const isActive = pathname === '/dashboard' ? item.href === '/' : pathname === fullHref;
    ```

    This logic should work, but verify:
    1. pathname comes from usePathname() hook
    2. Active items get: 'bg-primary text-primary-foreground'
    3. Inactive items get: 'text-muted-foreground hover:bg-muted hover:text-foreground'

    Potential issues:
    1. pathname might include or exclude trailing slashes inconsistently
    2. Route groups like (dashboard) might affect pathname

    Fix if needed:
    - Normalize pathname comparison (remove trailing slashes)
    - Use startsWith for nested routes: pathname.startsWith(fullHref)

    Improved active detection:
    ```typescript
    const isActive = item.href === '/'
      ? pathname === '/dashboard' || pathname === '/dashboard/'
      : pathname === fullHref || pathname.startsWith(fullHref + '/');
    ```

    Ensure visual contrast is strong enough:
    - Active: Bold background color (bg-primary)
    - Hover: Subtle highlight (bg-muted)
  </action>
  <verify>Current page is clearly highlighted in sidebar; navigation to different pages updates highlight</verify>
  <done>Active page indicator clearly visible in sidebar</done>
</task>

<task type="auto">
  <name>Task 3: Verify mobile menu functionality</name>
  <files>ui-next/src/components/header.tsx</files>
  <action>
    Verify mobile hamburger menu works correctly.

    Current implementation in header.tsx:
    - Sheet component with SheetTrigger (hamburger button)
    - SheetContent contains Sidebar component
    - Only visible on mobile (md:hidden on trigger)

    Check and fix:
    1. Sheet trigger button has correct size and is clickable
    2. Sheet opens from left side with full Sidebar content
    3. Clicking a link in mobile menu closes the Sheet and navigates

    Potential issue: Links in mobile Sheet might not close the Sheet after navigation.

    If Sheet doesn't close on navigation:
    - The Sheet component should auto-close when route changes
    - Or add onOpenChange handler that detects pathname changes

    Verify by:
    1. Resize browser to mobile width (<768px)
    2. Click hamburger menu - should open
    3. Click a navigation link - should navigate AND close menu
    4. Menu should be accessible via hamburger again
  </action>
  <verify>Mobile menu opens, shows navigation, links work, menu closes after navigation</verify>
  <done>Mobile menu functionality verified and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Desktop: Sidebar links clickable and navigate correctly
- [ ] Desktop: Active page clearly highlighted in sidebar
- [ ] Mobile: Hamburger menu button visible
- [ ] Mobile: Menu opens when tapped
- [ ] Mobile: Links navigate and close menu
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- Navigation works on both desktop and mobile
- Active state indicator clearly visible
- No JavaScript console errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-infrastructure/12-03-SUMMARY.md`
</output>
