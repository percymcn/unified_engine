---
phase: 05-infrastructure-adapters
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - app/infrastructure/repositories/signal_repository.py
  - app/infrastructure/repositories/trade_repository.py
  - app/infrastructure/repositories/order_repository.py
  - app/infrastructure/repositories/account_repository.py
  - app/infrastructure/repositories/position_repository.py
  - app/infrastructure/repositories/__init__.py
autonomous: true

must_haves:
  truths:
    - "All 5 repository interfaces from domain ports are implemented"
    - "Repositories use SQLAlchemy for persistence"
    - "Repositories use mappers for ORM ↔ domain conversion"
  artifacts:
    - path: "app/infrastructure/repositories/signal_repository.py"
      provides: "SignalRepository implementation"
      min_lines: 80
    - path: "app/infrastructure/repositories/trade_repository.py"
      provides: "TradeRepository implementation"
      min_lines: 60
    - path: "app/infrastructure/repositories/order_repository.py"
      provides: "OrderRepository implementation"
      min_lines: 80
    - path: "app/infrastructure/repositories/account_repository.py"
      provides: "AccountRepository implementation"
      min_lines: 80
    - path: "app/infrastructure/repositories/position_repository.py"
      provides: "PositionRepository implementation"
      min_lines: 60
  key_links:
    - from: "app/infrastructure/repositories/signal_repository.py"
      to: "app/domain/ports/repository_port.py"
      via: "implements SignalRepository interface"
      pattern: "class SQLAlchemySignalRepository.*SignalRepository"
    - from: "app/infrastructure/repositories/signal_repository.py"
      to: "app/infrastructure/mappers/signal_mapper.py"
      via: "uses mapper for conversion"
      pattern: "SignalMapper"
---

<objective>
Implement SQLAlchemy-based repositories for all domain repository interfaces.

Purpose: Provide concrete persistence for domain entities using PostgreSQL.
Output: 5 repository implementations that use mappers for ORM ↔ domain conversion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Repository interfaces to implement:
@app/domain/ports/repository_port.py

# Mappers for conversion (from 05-02):
@app/infrastructure/mappers/__init__.py

# ORM models:
@app/models/database_models.py

# Existing database setup:
@app/db/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SignalRepository implementation</name>
  <files>app/infrastructure/repositories/signal_repository.py</files>
  <action>
    Create SQLAlchemySignalRepository implementing SignalRepository port:

    ```python
    class SQLAlchemySignalRepository(SignalRepository):
        def __init__(self, session: AsyncSession):
            self._session = session
            self._mapper = SignalMapper()

        async def save(self, signal: Signal) -> Signal: ...
        async def delete(self, signal: Signal) -> None: ...
        async def get_by_id(self, signal_id: SignalId) -> Optional[Signal]: ...
        async def get_by_source(self, source: SignalSource) -> List[Signal]: ...
        async def get_pending(self) -> List[Signal]: ...
        async def get_recent(self, limit: int = 100) -> List[Signal]: ...
    ```

    Use SignalMapper for all ORM ↔ domain conversions.
    Use SQLAlchemy async patterns (select, insert, update).
  </action>
  <verify>python3 -c "from app.infrastructure.repositories.signal_repository import SQLAlchemySignalRepository; print('SignalRepository implemented')"</verify>
  <done>Signal repository with SQLAlchemy persistence</done>
</task>

<task type="auto">
  <name>Task 2: Create TradeRepository implementation</name>
  <files>app/infrastructure/repositories/trade_repository.py</files>
  <action>
    Create SQLAlchemyTradeRepository implementing TradeRepository port:

    ```python
    class SQLAlchemyTradeRepository(TradeRepository):
        async def save(self, trade: Trade) -> Trade: ...
        async def delete(self, trade: Trade) -> None: ...
        async def get_by_id(self, trade_id: TradeId) -> Optional[Trade]: ...
        async def get_by_order(self, order_id: OrderId) -> List[Trade]: ...
        async def get_by_account(self, account_id: AccountId) -> List[Trade]: ...
        async def get_recent(self, account_id: AccountId, limit: int) -> List[Trade]: ...
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.repositories.trade_repository import SQLAlchemyTradeRepository; print('TradeRepository implemented')"</verify>
  <done>Trade repository with SQLAlchemy persistence</done>
</task>

<task type="auto">
  <name>Task 3: Create OrderRepository implementation</name>
  <files>app/infrastructure/repositories/order_repository.py</files>
  <action>
    Create SQLAlchemyOrderRepository implementing OrderRepository port:

    ```python
    class SQLAlchemyOrderRepository(OrderRepository):
        async def save(self, order: Order) -> Order: ...
        async def delete(self, order: Order) -> None: ...
        async def get_by_id(self, order_id: OrderId) -> Optional[Order]: ...
        async def get_by_account(self, account_id: AccountId) -> List[Order]: ...
        async def get_pending(self, account_id: AccountId) -> List[Order]: ...
        async def get_by_status(self, account_id: AccountId, status: OrderStatus) -> List[Order]: ...
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.repositories.order_repository import SQLAlchemyOrderRepository; print('OrderRepository implemented')"</verify>
  <done>Order repository with SQLAlchemy persistence</done>
</task>

<task type="auto">
  <name>Task 4: Create AccountRepository implementation</name>
  <files>app/infrastructure/repositories/account_repository.py</files>
  <action>
    Create SQLAlchemyAccountRepository implementing AccountRepository port:

    ```python
    class SQLAlchemyAccountRepository(AccountRepository):
        async def save(self, account: Account) -> Account: ...
        async def delete(self, account: Account) -> None: ...
        async def get_by_id(self, account_id: AccountId) -> Optional[Account]: ...
        async def get_by_user(self, user_id: UserId) -> List[Account]: ...
        async def get_by_broker(self, broker: BrokerType) -> List[Account]: ...
        async def get_active(self, user_id: UserId) -> List[Account]: ...
        async def get_connected(self) -> List[Account]: ...
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.repositories.account_repository import SQLAlchemyAccountRepository; print('AccountRepository implemented')"</verify>
  <done>Account repository with SQLAlchemy persistence</done>
</task>

<task type="auto">
  <name>Task 5: Create PositionRepository implementation</name>
  <files>app/infrastructure/repositories/position_repository.py</files>
  <action>
    Create SQLAlchemyPositionRepository implementing PositionRepository port:

    ```python
    class SQLAlchemyPositionRepository(PositionRepository):
        async def save(self, position: Position) -> Position: ...
        async def delete(self, position: Position) -> None: ...
        async def get_by_id(self, position_id: PositionId) -> Optional[Position]: ...
        async def get_by_account(self, account_id: AccountId) -> List[Position]: ...
        async def get_open(self, account_id: AccountId) -> List[Position]: ...
        async def get_by_symbol(self, account_id: AccountId, symbol: Symbol) -> List[Position]: ...
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.repositories.position_repository import SQLAlchemyPositionRepository; print('PositionRepository implemented')"</verify>
  <done>Position repository with SQLAlchemy persistence</done>
</task>

<task type="auto">
  <name>Task 6: Update repositories __init__ exports</name>
  <files>app/infrastructure/repositories/__init__.py</files>
  <action>
    Export all repository implementations:

    ```python
    from app.infrastructure.repositories.signal_repository import SQLAlchemySignalRepository
    from app.infrastructure.repositories.trade_repository import SQLAlchemyTradeRepository
    from app.infrastructure.repositories.order_repository import SQLAlchemyOrderRepository
    from app.infrastructure.repositories.account_repository import SQLAlchemyAccountRepository
    from app.infrastructure.repositories.position_repository import SQLAlchemyPositionRepository

    __all__ = [
        "SQLAlchemySignalRepository",
        "SQLAlchemyTradeRepository",
        "SQLAlchemyOrderRepository",
        "SQLAlchemyAccountRepository",
        "SQLAlchemyPositionRepository",
    ]
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.repositories import SQLAlchemySignalRepository, SQLAlchemyAccountRepository; print('Repositories exported')"</verify>
  <done>All repositories accessible from package</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 repository implementations exist
- [ ] Each implements corresponding interface from repository_port.py
- [ ] Each uses mapper for ORM ↔ domain conversion
- [ ] Each accepts AsyncSession in constructor
- [ ] `python3 -c "from app.infrastructure.repositories import *"` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Repositories implement all methods from port interfaces
- No domain logic in repositories (pure persistence)
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure-adapters/05-03-SUMMARY.md`
</output>
