---
phase: 05-infrastructure-adapters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/infrastructure/mappers/__init__.py
  - app/infrastructure/mappers/signal_mapper.py
  - app/infrastructure/mappers/trade_mapper.py
  - app/infrastructure/mappers/account_mapper.py
  - app/infrastructure/mappers/position_mapper.py
  - app/infrastructure/mappers/order_mapper.py
autonomous: true

must_haves:
  truths:
    - "Mappers convert ORM models to domain entities and back"
    - "All 5 entity types have bidirectional mappers"
  artifacts:
    - path: "app/infrastructure/mappers/__init__.py"
      provides: "Mapper exports"
    - path: "app/infrastructure/mappers/signal_mapper.py"
      provides: "Signal ORM ↔ domain conversion"
      min_lines: 40
    - path: "app/infrastructure/mappers/trade_mapper.py"
      provides: "Trade ORM ↔ domain conversion"
      min_lines: 40
    - path: "app/infrastructure/mappers/account_mapper.py"
      provides: "Account ORM ↔ domain conversion"
      min_lines: 50
    - path: "app/infrastructure/mappers/position_mapper.py"
      provides: "Position ORM ↔ domain conversion"
      min_lines: 40
    - path: "app/infrastructure/mappers/order_mapper.py"
      provides: "Order ORM ↔ domain conversion"
      min_lines: 50
  key_links:
    - from: "app/infrastructure/mappers/signal_mapper.py"
      to: "app/domain/entities/signal.py"
      via: "imports Signal entity"
      pattern: "from app.domain.entities"
    - from: "app/infrastructure/mappers/signal_mapper.py"
      to: "app/models/database_models.py"
      via: "imports ORM model"
      pattern: "from app.models"
---

<objective>
Create bidirectional mappers between SQLAlchemy ORM models and domain entities.

Purpose: Isolate persistence concerns - repositories use these to convert between database and domain.
Output: Mapper classes for Signal, Trade, Account, Position, Order entities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Domain entities to map:
@app/domain/entities/signal.py
@app/domain/entities/trade.py
@app/domain/entities/account.py
@app/domain/entities/position.py
@app/domain/entities/order.py
@app/domain/value_objects.py
@app/domain/enums.py

# ORM models to map from:
@app/models/database_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mappers package</name>
  <files>app/infrastructure/mappers/__init__.py</files>
  <action>
    Create the mappers subpackage with exports.

    ```python
    """
    Infrastructure mappers - bidirectional conversion between ORM and domain.

    Mappers isolate persistence details from domain entities:
    - to_entity(): ORM model → domain entity
    - to_model(): domain entity → ORM model (for inserts/updates)
    """
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.mappers import *; print('Mappers package created')"</verify>
  <done>Mappers package structure in place</done>
</task>

<task type="auto">
  <name>Task 2: Create Signal mapper</name>
  <files>app/infrastructure/mappers/signal_mapper.py</files>
  <action>
    Create SignalMapper with:
    - to_entity(orm_model) → Signal domain entity
    - to_model(entity, orm_model=None) → ORM model (create or update)

    Handle:
    - SignalSource enum conversion (str → enum)
    - SignalAction enum conversion
    - SignalStatus enum conversion
    - Optional fields (symbol, volume, price, etc.)
    - Timestamps (datetime handling)

    CRITICAL: Domain entities use value objects (Symbol, Volume, Price).
    ORM models use primitives (str, float). Mapper handles conversion.
  </action>
  <verify>python3 -c "from app.infrastructure.mappers.signal_mapper import SignalMapper; print('SignalMapper created')"</verify>
  <done>Signal mapper with bidirectional conversion</done>
</task>

<task type="auto">
  <name>Task 3: Create Trade mapper</name>
  <files>app/infrastructure/mappers/trade_mapper.py</files>
  <action>
    Create TradeMapper with:
    - to_entity(orm_model) → Trade domain entity
    - to_model(entity, orm_model=None) → ORM model

    Handle:
    - TradeStatus enum conversion
    - Order relationship (trade has order_id)
    - Position relationship (trade has position_id)
    - Decimal fields (volume, price, profit, commission)
    - Timestamps
  </action>
  <verify>python3 -c "from app.infrastructure.mappers.trade_mapper import TradeMapper; print('TradeMapper created')"</verify>
  <done>Trade mapper with bidirectional conversion</done>
</task>

<task type="auto">
  <name>Task 4: Create Account mapper</name>
  <files>app/infrastructure/mappers/account_mapper.py</files>
  <action>
    Create AccountMapper with:
    - to_entity(orm_model) → Account domain entity
    - to_model(entity, orm_model=None) → ORM model

    Handle:
    - BrokerType enum conversion
    - Money value object for balance (domain uses Money, ORM uses Decimal)
    - Credentials (encrypted_api_key, encrypted_api_secret in ORM)
    - Boolean flags (is_active, is_connected, is_demo)
    - Account metadata (account_number, broker_account_id)
  </action>
  <verify>python3 -c "from app.infrastructure.mappers.account_mapper import AccountMapper; print('AccountMapper created')"</verify>
  <done>Account mapper with bidirectional conversion</done>
</task>

<task type="auto">
  <name>Task 5: Create Position mapper</name>
  <files>app/infrastructure/mappers/position_mapper.py</files>
  <action>
    Create PositionMapper with:
    - to_entity(orm_model) → Position domain entity
    - to_model(entity, orm_model=None) → ORM model

    Handle:
    - PositionSide enum conversion
    - Symbol value object
    - Volume value object
    - Price value object (entry_price, current_price)
    - Optional Decimal fields (stop_loss, take_profit, unrealized_pnl)
    - Timestamps (opened_at, closed_at)
  </action>
  <verify>python3 -c "from app.infrastructure.mappers.position_mapper import PositionMapper; print('PositionMapper created')"</verify>
  <done>Position mapper with bidirectional conversion</done>
</task>

<task type="auto">
  <name>Task 6: Create Order mapper</name>
  <files>app/infrastructure/mappers/order_mapper.py</files>
  <action>
    Create OrderMapper with:
    - to_entity(orm_model) → Order domain entity
    - to_model(entity, orm_model=None) → ORM model

    Handle:
    - OrderType enum conversion
    - OrderStatus enum conversion
    - OrderSide enum conversion
    - Symbol, Volume, Price value objects
    - Optional fields (stop_loss, take_profit, filled_price, filled_volume)
    - Timestamps (created_at, filled_at, cancelled_at)
    - Account relationship (order has account_id)
  </action>
  <verify>python3 -c "from app.infrastructure.mappers.order_mapper import OrderMapper; print('OrderMapper created')"</verify>
  <done>Order mapper with bidirectional conversion</done>
</task>

<task type="auto">
  <name>Task 7: Update mappers __init__ with exports</name>
  <files>app/infrastructure/mappers/__init__.py</files>
  <action>
    Update __init__.py to export all mappers:

    ```python
    from app.infrastructure.mappers.signal_mapper import SignalMapper
    from app.infrastructure.mappers.trade_mapper import TradeMapper
    from app.infrastructure.mappers.account_mapper import AccountMapper
    from app.infrastructure.mappers.position_mapper import PositionMapper
    from app.infrastructure.mappers.order_mapper import OrderMapper

    __all__ = [
        "SignalMapper",
        "TradeMapper",
        "AccountMapper",
        "PositionMapper",
        "OrderMapper",
    ]
    ```
  </action>
  <verify>python3 -c "from app.infrastructure.mappers import SignalMapper, TradeMapper, AccountMapper, PositionMapper, OrderMapper; print('All mappers exported')"</verify>
  <done>All mappers accessible from package</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -c "from app.infrastructure.mappers import *"` succeeds
- [ ] All 5 mappers have to_entity() and to_model() methods
- [ ] Mappers import domain entities (not copy definitions)
- [ ] Mappers import ORM models (not copy definitions)
- [ ] Value objects (Symbol, Volume, Price, Money) converted correctly
- [ ] Enums converted between str and domain enum types
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Mappers provide clean conversion between persistence and domain
- No domain logic in mappers (pure data transformation)
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure-adapters/05-02-SUMMARY.md`
</output>
