---
phase: 07-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-next/package.json
  - ui-next/tsconfig.json
  - ui-next/tailwind.config.ts
  - ui-next/postcss.config.mjs
  - ui-next/components.json
  - ui-next/src/app/globals.css
  - ui-next/src/app/layout.tsx
  - ui-next/src/lib/utils.ts
  - ui-next/src/components/ui/button.tsx
  - ui-next/src/components/ui/input.tsx
  - ui-next/src/components/ui/card.tsx
  - ui-next/src/components/ui/label.tsx
autonomous: true
must_haves:
  truths:
    - "Next.js 14 app starts with npm run dev"
    - "Dark theme applied globally (dark background, light text)"
    - "shadcn Button, Input, Card, Label components render correctly"
  artifacts:
    - path: "ui-next/package.json"
      provides: "Next.js 14 dependencies"
      contains: "next"
    - path: "ui-next/tailwind.config.ts"
      provides: "Tailwind with dark mode class"
      contains: "darkMode"
    - path: "ui-next/components.json"
      provides: "shadcn configuration"
      contains: "style"
    - path: "ui-next/src/app/globals.css"
      provides: "CSS variables for dark theme"
      contains: "--background"
    - path: "ui-next/src/components/ui/button.tsx"
      provides: "shadcn Button component"
      exports: ["Button"]
  key_links:
    - from: "ui-next/src/app/layout.tsx"
      to: "ui-next/src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "ui-next/src/components/ui/button.tsx"
      to: "ui-next/src/lib/utils.ts"
      via: "cn utility import"
      pattern: "import.*cn.*from"
---

<objective>
Initialize Next.js 14 project with shadcn/ui and dark theme foundation.

Purpose: Create the base UI infrastructure that all subsequent UI work builds upon. A fresh Next.js 14 app router project with Tailwind CSS, shadcn/ui components, and dark theme configured.

Output: Working Next.js 14 app in `ui-next/` directory with dark theme and core shadcn components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing UI reference (not to be modified, just for patterns):
# The existing ui/ directory uses React/Vite with Radix primitives
# The new ui-next/ will use Next.js 14 app router with shadcn/ui

# Backend auth endpoint (for later integration):
# POST /api/auth/login with username/password returns JWT
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 project</name>
  <files>ui-next/package.json, ui-next/tsconfig.json, ui-next/next.config.ts</files>
  <action>
    Create a new Next.js 14 project in `ui-next/` directory using the app router:

    1. Run: `npx create-next-app@14 ui-next --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack`
       - Use TypeScript: Yes
       - Use ESLint: Yes
       - Use Tailwind CSS: Yes
       - Use `src/` directory: Yes
       - Use App Router: Yes (CRITICAL - must use app router)
       - Customize import alias: @/*

    2. After creation, verify package.json has:
       - "next": "^14.x" (NOT Next.js 15)
       - "react": "^18.x"
       - "tailwindcss": present

    AVOID: Do not use --turbo flag (causes issues). Do not accept Next.js 15.
  </action>
  <verify>cd ui-next && npm run build succeeds</verify>
  <done>Next.js 14 project created with app router, TypeScript, and Tailwind</done>
</task>

<task type="auto">
  <name>Task 2: Configure shadcn/ui with dark theme</name>
  <files>ui-next/components.json, ui-next/tailwind.config.ts, ui-next/src/app/globals.css, ui-next/src/lib/utils.ts</files>
  <action>
    Initialize shadcn/ui and configure dark theme:

    1. Run in ui-next/: `npx shadcn@latest init`
       - Style: Default
       - Base color: Slate (for professional dark look)
       - CSS variables: Yes
       - Tailwind config path: tailwind.config.ts
       - CSS path: src/app/globals.css
       - React Server Components: Yes
       - Components path: @/components

    2. Update tailwind.config.ts to ensure darkMode: "class" is set

    3. Update globals.css to set dark theme as default:
       - The :root should have dark theme colors
       - Background: near-black (#0a0a0a or similar)
       - Foreground: light gray/white
       - Primary: cyan/teal accent (#00ffc2 from existing UI or shadcn default)
       - Card, popover backgrounds: slightly lighter than main bg

    4. Ensure ui-next/src/lib/utils.ts exists with cn() utility

    AVOID: Don't use light theme as default. Match the existing ui/ dark aesthetic.
  </action>
  <verify>cat ui-next/components.json shows valid config, cat ui-next/tailwind.config.ts shows darkMode setting</verify>
  <done>shadcn/ui initialized with dark theme CSS variables</done>
</task>

<task type="auto">
  <name>Task 3: Add core shadcn components</name>
  <files>ui-next/src/components/ui/button.tsx, ui-next/src/components/ui/input.tsx, ui-next/src/components/ui/card.tsx, ui-next/src/components/ui/label.tsx</files>
  <action>
    Add the essential shadcn components needed for auth and dashboard:

    1. Run in ui-next/:
       - npx shadcn@latest add button
       - npx shadcn@latest add input
       - npx shadcn@latest add card
       - npx shadcn@latest add label

    2. Verify each component file exists in ui-next/src/components/ui/

    3. Update root layout (ui-next/src/app/layout.tsx):
       - Set html lang="en" and className="dark" (force dark mode)
       - Body should have min-h-screen and bg-background classes
       - Keep the existing font configuration

    These 4 components are the minimum needed for login form and dashboard cards.
  </action>
  <verify>ls ui-next/src/components/ui/ shows button.tsx, input.tsx, card.tsx, label.tsx</verify>
  <done>Core shadcn components installed and root layout configured for dark mode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui-next && npm run dev` starts without errors
- [ ] Visiting http://localhost:3000 shows dark-themed page
- [ ] `npm run build` succeeds without TypeScript errors
- [ ] All 4 shadcn components exist in src/components/ui/
- [ ] globals.css has CSS variables for dark theme colors
</verification>

<success_criteria>
- All tasks completed
- Next.js 14 app runs with dark theme
- shadcn/ui Button, Input, Card, Label components available
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation/07-01-SUMMARY.md`
</output>
