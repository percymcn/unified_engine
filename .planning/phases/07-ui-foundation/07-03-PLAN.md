---
phase: 07-ui-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - ui-next/src/app/(dashboard)/layout.tsx
  - ui-next/src/app/(dashboard)/page.tsx
  - ui-next/src/components/sidebar.tsx
  - ui-next/src/components/header.tsx
  - ui-next/src/components/user-nav.tsx
  - ui-next/src/components/ui/avatar.tsx
  - ui-next/src/components/ui/dropdown-menu.tsx
  - ui-next/src/components/ui/separator.tsx
autonomous: false
must_haves:
  truths:
    - "Dashboard layout displays with sidebar navigation"
    - "Sidebar shows navigation links (Dashboard, Accounts, Signals, Settings)"
    - "Header shows user info and logout option"
    - "Logout clears session and redirects to login"
    - "Layout is responsive (sidebar collapses on mobile)"
  artifacts:
    - path: "ui-next/src/app/(dashboard)/layout.tsx"
      provides: "Dashboard shell with sidebar and header"
      min_lines: 30
    - path: "ui-next/src/app/(dashboard)/page.tsx"
      provides: "Dashboard home page"
      min_lines: 20
    - path: "ui-next/src/components/sidebar.tsx"
      provides: "Navigation sidebar"
      min_lines: 40
    - path: "ui-next/src/components/header.tsx"
      provides: "Top header with user nav"
      min_lines: 20
  key_links:
    - from: "ui-next/src/components/user-nav.tsx"
      to: "/api/auth/logout"
      via: "logout button fetch"
      pattern: "api/auth/logout"
    - from: "ui-next/src/app/(dashboard)/layout.tsx"
      to: "ui-next/src/components/sidebar.tsx"
      via: "component import"
      pattern: "import.*Sidebar"
---

<objective>
Create the dashboard layout with sidebar navigation and header.

Purpose: Provide the shell that all dashboard pages will render within. Users navigate via sidebar, see their info in header, and can logout. This completes the UI Foundation phase.

Output: Working dashboard layout with navigation, ready for dashboard content in Phase 8.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-foundation/07-01-SUMMARY.md
@.planning/phases/07-ui-foundation/07-02-SUMMARY.md

# Navigation structure for trading dashboard:
# - Dashboard (home/overview)
# - Accounts (broker accounts management - Phase 9)
# - Signals (signal status - Phase 8)
# - Trades (trade logs - Phase 8)
# - Settings (future)

# Icons: Use lucide-react (already in shadcn deps)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additional shadcn components for layout</name>
  <files>ui-next/src/components/ui/avatar.tsx, ui-next/src/components/ui/dropdown-menu.tsx, ui-next/src/components/ui/separator.tsx, ui-next/src/components/ui/sheet.tsx, ui-next/src/components/ui/tooltip.tsx</files>
  <action>
    Add shadcn components needed for the dashboard layout:

    1. Run in ui-next/:
       - npx shadcn@latest add avatar
       - npx shadcn@latest add dropdown-menu
       - npx shadcn@latest add separator
       - npx shadcn@latest add sheet (for mobile sidebar)
       - npx shadcn@latest add tooltip

    2. Verify all components installed in ui-next/src/components/ui/

    These components enable:
    - Avatar: User profile picture placeholder
    - DropdownMenu: User menu with logout
    - Separator: Visual dividers
    - Sheet: Mobile sidebar drawer
    - Tooltip: Navigation icon hints
  </action>
  <verify>ls ui-next/src/components/ui/ | grep -E "avatar|dropdown|separator|sheet|tooltip"</verify>
  <done>Additional shadcn components installed for layout</done>
</task>

<task type="auto">
  <name>Task 2: Create sidebar navigation component</name>
  <files>ui-next/src/components/sidebar.tsx</files>
  <action>
    Create the sidebar navigation component:

    1. Create ui-next/src/components/sidebar.tsx:
       - 'use client' directive (needs usePathname for active state)
       - Fixed-width sidebar (w-64) with dark background
       - App logo/name at top
       - Navigation items with icons (lucide-react):
         - LayoutDashboard icon -> Dashboard (/)
         - Users icon -> Accounts (/accounts)
         - Signal icon -> Signals (/signals)
         - History icon -> Trades (/trades)
         - Settings icon -> Settings (/settings)
       - Active state styling (highlight current route)
       - Hover effects
       - Use Link from next/link for navigation

    2. Styling:
       - Dark background matching theme (bg-card or similar)
       - Border-right for separation
       - Proper padding and spacing
       - Icons + text for each nav item
       - Active item: accent background color

    Use cn() utility for conditional classes. Match the dark aesthetic.
  </action>
  <verify>cat ui-next/src/components/sidebar.tsx shows nav items with icons and Link components</verify>
  <done>Sidebar component created with navigation links</done>
</task>

<task type="auto">
  <name>Task 3: Create header with user navigation</name>
  <files>ui-next/src/components/header.tsx, ui-next/src/components/user-nav.tsx</files>
  <action>
    Create header and user navigation components:

    1. Create ui-next/src/components/user-nav.tsx:
       - 'use client' directive
       - Avatar with user initials (fallback if no image)
       - DropdownMenu on click:
         - User email/name display
         - Separator
         - Logout option
       - Logout handler: POST to /api/auth/logout, then router.push('/login')

    2. Create ui-next/src/components/header.tsx:
       - Full-width header at top
       - Left: Mobile menu button (Sheet trigger for sidebar on mobile)
       - Center/Left: Page title or breadcrumb area (can be empty for now)
       - Right: UserNav component
       - Border-bottom for separation
       - Fixed height (h-16)

    3. For mobile menu:
       - Use Sheet component
       - Trigger button only visible on mobile (md:hidden)
       - Sheet content shows sidebar navigation
  </action>
  <verify>cat ui-next/src/components/header.tsx shows UserNav import and mobile menu trigger</verify>
  <done>Header and user navigation created with logout functionality</done>
</task>

<task type="auto">
  <name>Task 4: Create dashboard layout and home page</name>
  <files>ui-next/src/app/(dashboard)/layout.tsx, ui-next/src/app/(dashboard)/page.tsx</files>
  <action>
    Create the dashboard layout using route groups:

    1. Create ui-next/src/app/(dashboard)/layout.tsx:
       - Server component (no 'use client' needed at layout level)
       - Import Sidebar and Header components
       - Layout structure:
         ```
         <div className="flex h-screen">
           <Sidebar className="hidden md:flex" />
           <div className="flex-1 flex flex-col">
             <Header />
             <main className="flex-1 overflow-auto p-6">
               {children}
             </main>
           </div>
         </div>
         ```
       - Sidebar hidden on mobile (Sheet in header handles it)

    2. Create ui-next/src/app/(dashboard)/page.tsx:
       - Dashboard home page
       - Placeholder content:
         - Welcome heading
         - Cards showing placeholder stats (will be real in Phase 8):
           - Active Signals
           - Connected Brokers
           - Today's Trades
           - Account Balance
       - Use Card components from shadcn
       - Grid layout (2 columns on tablet, 4 on desktop)

    The (dashboard) folder with parentheses is a route group - it creates shared layout without affecting URL.
  </action>
  <verify>ls ui-next/src/app/\\(dashboard\\)/ shows layout.tsx and page.tsx</verify>
  <done>Dashboard layout and home page created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UI foundation with Next.js 14, auth flow, and dashboard layout</what-built>
  <how-to-verify>
    1. Start backend: cd /home/pharma5/unified_engine && python -m uvicorn app.main:app --reload
    2. Start frontend: cd /home/pharma5/unified_engine/ui-next && npm run dev
    3. Visit: http://localhost:3000 (should redirect to /login)
    4. Verify login page:
       - Dark theme with centered card
       - Username and password inputs
       - Login button
    5. Test login with a user (create one via backend if needed, or use test credentials)
    6. After login, verify dashboard:
       - Sidebar visible on desktop (links: Dashboard, Accounts, Signals, Trades, Settings)
       - Header at top with user menu
       - Click user menu -> see logout option
       - Dashboard shows placeholder cards
    7. Test logout -> returns to login page
    8. Resize to mobile -> sidebar collapses, hamburger menu appears
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui-next && npm run build` succeeds
- [ ] Dashboard layout renders with sidebar and header
- [ ] Navigation links work (Dashboard active by default)
- [ ] User menu shows logout option
- [ ] Logout clears session and redirects to login
- [ ] Mobile view shows hamburger menu instead of sidebar
- [ ] Human verification checkpoint passed
</verification>

<success_criteria>
- All tasks completed
- Dashboard layout functional with navigation
- Auth flow works end-to-end (login -> dashboard -> logout)
- Responsive layout (mobile sidebar collapse)
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation/07-03-SUMMARY.md`
</output>
