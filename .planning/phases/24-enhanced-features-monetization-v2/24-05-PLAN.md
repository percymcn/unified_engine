---
phase: 24-enhanced-features-monetization-v2
plan: 05
type: execute
wave: 2
depends_on: ["24-02"]
files_modified:
  - ui-next/src/components/landing/pricing-section.tsx
  - ui-next/src/app/api/billing/plans/route.ts
  - ui-next/src/components/pricing/pricing-card.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page shows all 4 pricing tiers"
    - "Each tier shows price, broker limit, and features"
    - "CTA buttons link to correct checkout with tier_id"
    - "Current user's tier is highlighted if logged in"
  artifacts:
    - path: "ui-next/src/components/pricing/pricing-card.tsx"
      provides: "Reusable pricing card component"
      min_lines: 50
  key_links:
    - from: "ui-next/src/components/landing/pricing-section.tsx"
      to: "ui-next/src/lib/pricing.ts"
      via: "import PRICING_TIERS"
      pattern: "PRICING_TIERS"
---

<objective>
Update landing page pricing section with 4-tier pricing display.

Purpose: Show complete pricing options to visitors (BILL-09, LAND-05 update).
Output: 4-tier pricing section on landing page and reusable pricing components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/24-enhanced-features-monetization-v2/24-02-PLAN.md (4-tier backend)

Relevant source files:
@ui-next/src/components/landing/pricing-section.tsx (existing pricing section)
@ui-next/src/lib/pricing.ts (pricing constants from 24-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable pricing card component</name>
  <files>ui-next/src/components/pricing/pricing-card.tsx</files>
  <action>
    1. Create ui-next/src/components/pricing/pricing-card.tsx:
       - Props: tier (TierConfig), isPopular (boolean), isCurrent (boolean), onSelect (function)
       - Display: tier name, price (/mo), broker limit badge
       - Feature list with checkmarks
       - CTA button: "Get Started" / "Current Plan" / "Upgrade"
       - Popular tier gets highlighted border/badge
       - Current tier shows "Current Plan" state
    2. Tier features to display:
       - "{X} Broker Connection(s)"
       - "Unlimited Signals"
       - "Real-time Execution"
       - "Risk Management"
       - "24/7 Support" (tier_3, tier_4 only)
       - "Priority Support" (tier_4 only)
    3. Use shadcn Card component as base
    4. Responsive: Stack on mobile, row on desktop
  </action>
  <verify>npm run build succeeds</verify>
  <done>Reusable pricing card component created</done>
</task>

<task type="auto">
  <name>Task 2: Update landing page pricing section</name>
  <files>ui-next/src/components/landing/pricing-section.tsx</files>
  <action>
    1. Update pricing-section.tsx to display 4 tiers:
       - Import PRICING_TIERS from lib/pricing.ts
       - Import PricingCard component
       - Map over tiers to render PricingCard for each
       - Mark tier_3 as "Most Popular"
       - Free trial callout: "Start with 100 free trades"
    2. CTA links:
       - For unauthenticated: /register?tier={tier_id}
       - For authenticated free: /api/billing/checkout?tier={tier_id}
       - For authenticated paid: /dashboard/settings/billing (manage subscription)
    3. Add annual pricing toggle (placeholder - shows 20% discount)
    4. Comparison table below cards (optional):
       - Feature matrix showing what each tier includes
  </action>
  <verify>Visit landing page, see 4 pricing tiers displayed</verify>
  <done>Landing page shows all 4 pricing tiers</done>
</task>

<task type="auto">
  <name>Task 3: Create plans BFF route</name>
  <files>ui-next/src/app/api/billing/plans/route.ts</files>
  <action>
    1. Create/update BFF route at ui-next/src/app/api/billing/plans/route.ts:
       - GET handler that returns all pricing tiers
       - Include features array for each tier
       - If user authenticated: include their current_tier
       - Return: { tiers: [...], current_tier: string | null }
    2. Used by both landing page and upgrade page
    3. Cache response (tiers don't change often)
  </action>
  <verify>curl http://localhost:3456/api/billing/plans returns all 4 tiers</verify>
  <done>BFF route provides pricing data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PricingCard component renders correctly
- [ ] Landing page shows all 4 tiers in a row
- [ ] tier_3 (Pro) marked as "Most Popular"
- [ ] Prices display correctly ($19.99, $39.99, $69.99, $129.99)
- [ ] Broker limits shown (1, 2, 3, 4)
- [ ] CTA buttons link to correct checkout URLs
- [ ] Responsive on mobile (stacked cards)
- [ ] /api/billing/plans returns tier data
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Pricing section is clear and conversion-focused
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-enhanced-features-monetization-v2/24-05-SUMMARY.md`
</output>
