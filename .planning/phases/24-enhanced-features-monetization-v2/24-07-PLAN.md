---
phase: 24-enhanced-features-monetization-v2
plan: 07
type: execute
wave: 2
depends_on: ["24-06"]
files_modified:
  - ui-next/src/app/api/accounts/available/[broker]/route.ts
  - ui-next/src/components/accounts/account-selector.tsx
  - ui-next/src/app/dashboard/accounts/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Checkbox list shows all available accounts per broker"
    - "User can select/deselect accounts"
    - "Multiple accounts can be selected simultaneously"
    - "Selection persists after page reload"
  artifacts:
    - path: "ui-next/src/components/accounts/account-selector.tsx"
      provides: "Multi-account checkbox selector"
      min_lines: 80
  key_links:
    - from: "ui-next/src/components/accounts/account-selector.tsx"
      to: "/api/accounts/available/{broker}"
      via: "fetch available accounts"
      pattern: "fetch.*accounts.*available"
---

<objective>
Implement UI for selecting which broker accounts receive signals.

Purpose: Enable multi-account signal routing selection (ACCT-11, ACCT-12).
Output: Account selector component with checkbox list UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/24-enhanced-features-monetization-v2/24-06-PLAN.md (account fetcher backend)

Relevant source files:
@ui-next/src/app/dashboard/accounts/page.tsx (accounts list page)
@ui-next/src/components/ui/checkbox.tsx (shadcn checkbox)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BFF route for available accounts</name>
  <files>ui-next/src/app/api/accounts/available/[broker]/route.ts</files>
  <action>
    1. Create BFF route at ui-next/src/app/api/accounts/available/[broker]/route.ts:
       - GET handler that proxies to backend /api/accounts/available/{broker}
       - Return: { accounts: BrokerAccountInfo[], selected_ids: string[] }
       - Handle errors gracefully
    2. Create BFF route for selection toggle:
       - ui-next/src/app/api/accounts/[id]/select/route.ts
       - PUT handler that proxies to backend /api/accounts/{id}/select
  </action>
  <verify>curl http://localhost:3456/api/accounts/available/tradelocker returns accounts</verify>
  <done>BFF routes for account fetching and selection work</done>
</task>

<task type="auto">
  <name>Task 2: Create account selector component</name>
  <files>ui-next/src/components/accounts/account-selector.tsx</files>
  <action>
    1. Create ui-next/src/components/accounts/account-selector.tsx:
       - Props: brokerId (string), brokerType (string)
       - Fetch available accounts from /api/accounts/available/{broker}
       - Display checkbox list with account info:
         - [ ] Account Name (Type) - Balance: $X,XXX
         - Show login/server for MT4/MT5 accounts
       - Checkbox onChange: Call PUT /api/accounts/{id}/select
       - "Select All" / "Deselect All" buttons
       - Loading skeleton while fetching
       - Empty state: "No accounts found. Connect broker first."
    2. Use shadcn Checkbox component
    3. Group accounts by type (Live, Demo, Evaluation, etc.)
    4. Show selection count: "3 of 5 accounts selected"
  </action>
  <verify>npm run build succeeds</verify>
  <done>Account selector component with checkbox list created</done>
</task>

<task type="auto">
  <name>Task 3: Integrate selector into broker account page</name>
  <files>ui-next/src/app/dashboard/accounts/[id]/page.tsx</files>
  <action>
    1. Update broker account detail page to include AccountSelector:
       - Add "Select Accounts" section below connection status
       - Import and render AccountSelector component
       - Show only when broker is connected
    2. Add info tooltip:
       - "Selected accounts will receive trading signals. Deselect accounts you want to exclude."
    3. Add refresh button to re-sync accounts from broker:
       - Calls POST /api/accounts/sync
       - Shows toast on success/failure
    4. Visual feedback:
       - Checkmark icon next to "Receiving Signals" for selected accounts
       - Greyed out appearance for deselected accounts
  </action>
  <verify>Visit broker account page, see account selection checkboxes</verify>
  <done>Account selector integrated into broker management UI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] BFF route returns available accounts from backend
- [ ] Account selector shows checkbox list
- [ ] Checkboxes toggle account selection
- [ ] Selection persists after page reload
- [ ] "Select All" / "Deselect All" buttons work
- [ ] Selection count displayed correctly
- [ ] Accounts grouped by type
- [ ] Empty state handled gracefully
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Multi-account selection UI is intuitive
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-enhanced-features-monetization-v2/24-07-SUMMARY.md`
</output>
