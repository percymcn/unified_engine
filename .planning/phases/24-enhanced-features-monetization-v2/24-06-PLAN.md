---
phase: 24-enhanced-features-monetization-v2
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/account_fetcher_service.py
  - app/routers/accounts.py
  - app/models/models.py
autonomous: true

must_haves:
  truths:
    - "Each broker endpoint returns list of available accounts"
    - "Account includes: id, type, login, server, balance (where available)"
    - "Selected account IDs stored in database per user"
    - "Different broker ID formats handled dynamically"
  artifacts:
    - path: "app/services/account_fetcher_service.py"
      provides: "Multi-broker account fetcher"
      min_lines: 100
  key_links:
    - from: "app/routers/accounts.py"
      to: "app/services/account_fetcher_service.py"
      via: "fetch_accounts endpoint"
      pattern: "fetch_accounts|get_available_accounts"
---

<objective>
Implement backend endpoints to fetch available accounts from each broker SDK and store user selections.

Purpose: Enable users to select which accounts receive signals (ACCT-07 through ACCT-10, ACCT-13, ACCT-14).
Output: Account fetcher service and updated accounts router with selection storage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@app/routers/accounts.py (existing account management)
@app/services/tradelocker_service.py (TradeLocker SDK)
@app/services/projectx_service.py (TopStep/ProjectX SDK)
@app/services/tradovate_service.py (Tradovate SDK)
@app/services/metaapi_service.py (MetaAPI MT4/MT5 SDK)
@app/models/models.py (BrokerAccount model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create account fetcher service</name>
  <files>app/services/account_fetcher_service.py</files>
  <action>
    1. Create app/services/account_fetcher_service.py:
       - AccountFetcherService class with broker-specific methods:
         - fetch_tradelocker_accounts(credentials) -> List[BrokerAccountInfo]:
           - Use TradeLocker SDK to get all accounts (live, demo)
           - Return: id, name, type (live/demo), balance, currency
         - fetch_projectx_accounts(credentials) -> List[BrokerAccountInfo]:
           - Use ProjectX SDK to get accounts (live, evaluation, express)
           - Return: id, name, type, balance, currency
         - fetch_tradovate_accounts(credentials) -> List[BrokerAccountInfo]:
           - Use Tradovate SDK to get accounts
           - Return: id, name, type, active status
         - fetch_metaapi_accounts(credentials) -> List[BrokerAccountInfo]:
           - Use MetaAPI SDK to get connected accounts
           - Return: id, login, server, type (MT4/MT5), balance
       - BrokerAccountInfo dataclass:
         - id: str (broker-specific format)
         - name: str
         - account_type: str (live, demo, evaluation, express)
         - balance: Optional[float]
         - currency: Optional[str]
         - server: Optional[str] (for MT4/MT5)
         - login: Optional[str] (for MT4/MT5)
         - broker_type: str
    2. Unified fetch method:
       - fetch_all_accounts(user, broker_type) - Routes to correct fetcher
    3. Handle different broker ID formats:
       - TradeLocker: numeric IDs
       - ProjectX: alphanumeric IDs
       - Tradovate: numeric IDs
       - MetaAPI: UUID format
  </action>
  <verify>python -c "from app.services.account_fetcher_service import AccountFetcherService; print('OK')"</verify>
  <done>Account fetcher service supports all 4 broker types</done>
</task>

<task type="auto">
  <name>Task 2: Add account selection endpoints</name>
  <files>app/routers/accounts.py, app/models/models.py</files>
  <action>
    1. Add to BrokerAccount model (if not exists):
       - is_selected = Column(Boolean, default=True)  # Whether account receives signals
       - account_type = Column(String(50))  # live, demo, evaluation, express
       - external_account_id = Column(String(100))  # Broker's account ID
    2. Add endpoints to accounts.py:
       - GET /api/accounts/available/{broker_type}:
         - Fetch available accounts from broker SDK
         - Return list with selection status (cross-ref with stored BrokerAccounts)
       - PUT /api/accounts/{account_id}/select:
         - Toggle is_selected for account
         - Body: { "selected": boolean }
       - POST /api/accounts/sync:
         - Re-fetch accounts from all connected brokers
         - Update stored accounts (add new, mark removed as inactive)
    3. Create migration 017_add_account_selection.py (if needed):
       - Add is_selected, account_type, external_account_id columns
  </action>
  <verify>curl http://localhost:8765/api/accounts/available/tradelocker returns account list</verify>
  <done>Account selection endpoints allow fetching and toggling accounts</done>
</task>

<task type="auto">
  <name>Task 3: Update signal routing to respect selection</name>
  <files>app/services/signal_processor.py</files>
  <action>
    1. Update signal processor to filter by selected accounts:
       - In execute_signal method, filter target_accounts:
         - accounts = [a for a in accounts if a.is_selected]
       - Log if signal skipped due to no selected accounts
    2. Handle routing modes:
       - If signal specifies account_id: Use that specific account
       - If signal specifies broker_type: Use all selected accounts of that type
       - If neither: Use all selected accounts
    3. Add logging for routing decisions:
       - "Routing to {N} selected accounts for {broker_type}"
       - "Skipping account {id} (not selected)"
  </action>
  <verify>Toggle account selection, verify signal only routes to selected accounts</verify>
  <done>Signal processor respects account selection status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AccountFetcherService fetches accounts from all 4 broker types
- [ ] GET /api/accounts/available/{broker_type} returns account list
- [ ] Accounts include: id, name, type, balance (where available)
- [ ] PUT /api/accounts/{id}/select toggles selection
- [ ] Selected accounts stored in database
- [ ] Signal processor only routes to selected accounts
- [ ] Different broker ID formats handled correctly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Multi-broker account fetching works
- Account selection persists and affects signal routing
</success_criteria>

<output>
After completion, create `.planning/phases/24-enhanced-features-monetization-v2/24-06-SUMMARY.md`
</output>
