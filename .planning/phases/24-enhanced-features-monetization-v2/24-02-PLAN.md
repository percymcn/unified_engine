---
phase: 24-enhanced-features-monetization-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/billing.py
  - app/services/stripe_service.py
  - ui-next/src/lib/pricing.ts
autonomous: true

must_haves:
  truths:
    - "4 pricing tiers defined in backend"
    - "Each tier has broker limit (1, 2, 3, 4)"
    - "Checkout creates correct Stripe session for selected tier"
    - "Webhook updates user subscription_tier correctly"
  artifacts:
    - path: "app/services/stripe_service.py"
      provides: "4-tier Stripe product/price mapping"
      contains: "tier_1|tier_2|tier_3|tier_4"
    - path: "ui-next/src/lib/pricing.ts"
      provides: "Frontend pricing constants"
      min_lines: 40
  key_links:
    - from: "app/routers/billing.py"
      to: "app/services/stripe_service.py"
      via: "create_checkout_session with tier"
      pattern: "create_checkout.*tier"
---

<objective>
Implement 4-tier pricing structure in backend and Stripe integration.

Purpose: Enable tiered monetization with broker count limits (BILL-08, BILL-10, BILL-11).
Output: 4-tier pricing backend with Stripe product mapping and broker gating.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@app/routers/billing.py (existing billing endpoints)
@app/models/models.py (User with subscription_tier)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define 4-tier pricing structure</name>
  <files>app/services/stripe_service.py, ui-next/src/lib/pricing.ts</files>
  <action>
    1. Create/update app/services/stripe_service.py:
       - PRICING_TIERS dict:
         ```python
         PRICING_TIERS = {
             "tier_1": {"name": "Starter", "price": 1999, "brokers": 1, "stripe_price_id": "price_tier1_monthly"},
             "tier_2": {"name": "Growth", "price": 3999, "brokers": 2, "stripe_price_id": "price_tier2_monthly"},
             "tier_3": {"name": "Pro", "price": 6999, "brokers": 3, "stripe_price_id": "price_tier3_monthly"},
             "tier_4": {"name": "Enterprise", "price": 12999, "brokers": 4, "stripe_price_id": "price_tier4_monthly"},
         }
         ```
       - get_tier_by_broker_count(broker_count) - Return appropriate tier
       - get_broker_limit(tier) - Return max brokers for tier
       - Functions: create_checkout_session(user, tier_id), get_or_create_customer(user)
    2. Create ui-next/src/lib/pricing.ts:
       - Export PRICING_TIERS matching backend
       - Export helper functions: formatPrice(cents), getTierFeatures(tier_id)
       - Tier features list for each tier
    3. Prices in cents (1999 = $19.99)
  </action>
  <verify>python -c "from app.services.stripe_service import PRICING_TIERS; print(PRICING_TIERS)"</verify>
  <done>4-tier pricing structure defined in backend and frontend</done>
</task>

<task type="auto">
  <name>Task 2: Update billing endpoints for 4-tier checkout</name>
  <files>app/routers/billing.py</files>
  <action>
    1. Update POST /api/billing/checkout endpoint:
       - Accept tier_id parameter (tier_1, tier_2, tier_3, tier_4)
       - Validate tier_id exists in PRICING_TIERS
       - Create Stripe checkout session with correct price_id
       - Include tier in metadata for webhook processing
    2. Update GET /api/billing/plans endpoint:
       - Return all 4 tiers with name, price, broker_limit, features
       - Include current user's tier for comparison
    3. Update GET /api/billing/status endpoint:
       - Return current_tier, broker_limit, brokers_used
       - Calculate brokers_used from user's active accounts
  </action>
  <verify>curl -X POST http://localhost:8765/api/billing/checkout -d '{"tier_id":"tier_2"}' returns Stripe URL</verify>
  <done>Billing endpoints support 4-tier checkout</done>
</task>

<task type="auto">
  <name>Task 3: Update webhook and broker gating</name>
  <files>app/routers/billing.py, app/routers/accounts.py</files>
  <action>
    1. Update Stripe webhook handler in billing.py:
       - On checkout.session.completed: Read tier from metadata
       - Set user.subscription_tier to tier_id (tier_1, tier_2, tier_3, tier_4)
       - Handle subscription.updated: Update tier if changed
       - Handle subscription.deleted: Set tier back to "free"
    2. Update broker slot checking in accounts.py:
       - require_broker_slot dependency:
         - Get user's subscription_tier
         - Get broker_limit from PRICING_TIERS (or 1 for free)
         - Count user's active broker accounts
         - Reject if at limit with 402 error + upgrade message
    3. Update User model if needed:
       - subscription_tier values: "free", "tier_1", "tier_2", "tier_3", "tier_4"
  </action>
  <verify>Test: tier_1 user cannot add more than 1 broker account</verify>
  <done>Webhook updates tier, broker gating enforces limits</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PRICING_TIERS has all 4 tiers with correct prices
- [ ] POST /api/billing/checkout accepts tier_id parameter
- [ ] GET /api/billing/plans returns all 4 tiers
- [ ] Webhook correctly sets subscription_tier from metadata
- [ ] Broker limit enforced: tier_1=1, tier_2=2, tier_3=3, tier_4=4
- [ ] Free tier limited to 1 broker (during trial)
- [ ] Frontend pricing.ts matches backend tiers
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 4-tier pricing works end-to-end
- Broker gating enforces tier limits
</success_criteria>

<output>
After completion, create `.planning/phases/24-enhanced-features-monetization-v2/24-02-SUMMARY.md`
</output>
