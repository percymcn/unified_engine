---
phase: 24-enhanced-features-monetization-v2
plan: 04
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - ui-next/src/components/dashboard/trial-status-widget.tsx
  - ui-next/src/app/api/trial/status/route.ts
  - ui-next/src/components/trial/upgrade-prompt.tsx
  - ui-next/src/app/dashboard/upgrade/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows remaining trades AND days"
    - "Trial exhausted shows upgrade prompt"
    - "User directed to pricing page when trial expired"
    - "Upgrade prompt is visually prominent but not blocking"
  artifacts:
    - path: "ui-next/src/components/trial/upgrade-prompt.tsx"
      provides: "Upgrade prompt component"
      min_lines: 40
    - path: "ui-next/src/app/dashboard/upgrade/page.tsx"
      provides: "Upgrade page with pricing"
      min_lines: 60
  key_links:
    - from: "ui-next/src/components/dashboard/trial-status-widget.tsx"
      to: "/api/trial/status"
      via: "fetch trial info"
      pattern: "fetch.*trial/status"
---

<objective>
Implement trial UI showing remaining trades/days and upgrade prompts when trial exhausted.

Purpose: Complete trial user experience (TRIAL-04, TRIAL-06, TRIAL-07).
Output: Trial status widget, upgrade prompts, and upgrade page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/24-enhanced-features-monetization-v2/24-01-PLAN.md (trial backend)

Relevant source files:
@ui-next/src/components/dashboard/trial-status-widget.tsx (existing widget)
@ui-next/src/app/dashboard/settings/billing/page.tsx (billing page pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BFF route for trial status</name>
  <files>ui-next/src/app/api/trial/status/route.ts</files>
  <action>
    1. Create BFF route at ui-next/src/app/api/trial/status/route.ts:
       - GET handler that proxies to backend /api/trial/status
       - Pass through Authorization cookie
       - Return: { status, trades_remaining, days_remaining, trial_started_at, is_expired }
       - Handle errors gracefully (return null trial info if not authenticated)
    2. Add types for TrialStatus response
  </action>
  <verify>curl http://localhost:3456/api/trial/status returns trial info</verify>
  <done>BFF route for trial status works</done>
</task>

<task type="auto">
  <name>Task 2: Update trial status widget with real data</name>
  <files>ui-next/src/components/dashboard/trial-status-widget.tsx</files>
  <action>
    1. Update trial-status-widget.tsx to fetch from /api/trial/status:
       - Use SWR or useEffect to fetch trial data
       - Display trades remaining: "{X} trades left" with progress bar
       - Display days remaining: "{X} days left" with progress bar
       - Show "Trial Active" badge when active
       - Show "Trial Expired" badge when expired with red styling
    2. For paid users (subscription_tier != "free"):
       - Show current tier name instead of trial status
       - Display broker limit: "Using {X}/{Y} brokers"
    3. Add "Upgrade" button that links to /dashboard/upgrade
    4. Use skeleton loading while data fetches
  </action>
  <verify>npm run build succeeds, widget shows real trial data</verify>
  <done>Trial widget shows remaining trades and days</done>
</task>

<task type="auto">
  <name>Task 3: Create upgrade prompt and page</name>
  <files>ui-next/src/components/trial/upgrade-prompt.tsx, ui-next/src/app/dashboard/upgrade/page.tsx</files>
  <action>
    1. Create ui-next/src/components/trial/upgrade-prompt.tsx:
       - Alert/banner component with upgrade message
       - Props: isExpired (boolean), tradesRemaining (number)
       - If expired: "Your trial has ended. Upgrade to continue trading."
       - If low trades (<10): "Only {X} trades left. Upgrade for unlimited."
       - CTA button: "View Plans" or "Upgrade Now"
       - Dismissible with X button (stores in localStorage)
    2. Create ui-next/src/app/dashboard/upgrade/page.tsx:
       - Full-page upgrade/pricing comparison
       - Import PRICING_TIERS from lib/pricing.ts
       - Display all 4 tiers with features
       - Highlight recommended tier based on current broker count
       - Each tier has "Select" button → /api/billing/checkout?tier=tier_X
       - Current tier shows "Current Plan" badge
    3. Add UpgradePrompt to dashboard layout when trial is low/expired
  </action>
  <verify>npm run build, /dashboard/upgrade page shows all 4 tiers</verify>
  <done>Upgrade prompts and page guide users to subscribe</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] /api/trial/status BFF route returns trial data
- [ ] Trial widget shows trades remaining with progress bar
- [ ] Trial widget shows days remaining with progress bar
- [ ] Expired trial shows red "Trial Expired" badge
- [ ] Low trial (<10 trades) shows warning prompt
- [ ] /dashboard/upgrade page displays all 4 pricing tiers
- [ ] Selecting tier redirects to Stripe checkout
- [ ] Paid users see their current tier instead of trial info
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Trial UI provides clear upgrade path
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-enhanced-features-monetization-v2/24-04-SUMMARY.md`
</output>
