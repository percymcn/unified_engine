---
phase: 23-user-settings-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["23-04"]
files_modified:
  - ui-next/src/components/dashboard/recent-executions-widget.tsx
  - ui-next/src/components/dashboard/equity-chart-widget.tsx
  - ui-next/src/components/dashboard/trial-status-widget.tsx
  - ui-next/src/components/dashboard/open-positions-widget.tsx
  - ui-next/src/app/dashboard/page.tsx
  - ui-next/src/app/api/dashboard/executions/route.ts
  - ui-next/src/app/api/dashboard/equity/route.ts
  - ui-next/src/app/api/dashboard/positions/route.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard shows last 10 trade executions"
    - "Equity chart displays balance history over time"
    - "Trial status shows remaining trades/days (placeholder if no trial)"
    - "Open positions widget shows current positions across accounts"
  artifacts:
    - path: "ui-next/src/components/dashboard/recent-executions-widget.tsx"
      provides: "Recent executions list"
      min_lines: 50
    - path: "ui-next/src/components/dashboard/equity-chart-widget.tsx"
      provides: "Balance history chart"
      min_lines: 60
    - path: "ui-next/src/components/dashboard/open-positions-widget.tsx"
      provides: "Current positions overview"
      min_lines: 50
  key_links:
    - from: "ui-next/src/app/dashboard/page.tsx"
      to: "ui-next/src/components/dashboard/recent-executions-widget.tsx"
      via: "import and render"
      pattern: "RecentExecutionsWidget"
---

<objective>
Add dashboard widgets for recent executions, equity chart, trial status, and open positions.

Purpose: Complete dashboard with key metrics and visualizations (DASH-06, DASH-07, DASH-08, DASH-09).
Output: Four new dashboard widgets providing comprehensive trading overview.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/23-user-settings-dashboard/23-04-SUMMARY.md (dashboard enhancements)

Relevant source files:
@ui-next/src/app/dashboard/page.tsx (dashboard layout)
@ui-next/src/components/dashboard/risk-usage-widget.tsx (widget pattern)
@ui-next/src/components/dashboard/rejected-signals-widget.tsx (widget pattern)
@app/models/models.py (ExecutionLog, Position, Trade models)
@app/routers/dashboard.py (existing dashboard endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recent executions widget</name>
  <files>ui-next/src/components/dashboard/recent-executions-widget.tsx, ui-next/src/app/api/dashboard/executions/route.ts, app/routers/dashboard.py</files>
  <action>
    1. Add backend endpoint in app/routers/dashboard.py:
       - GET /dashboard/executions?limit=10 - Return recent execution logs
       - Query ExecutionLog model, order by created_at desc, limit 10
       - Return: id, signal_id, account (broker, account_id), symbol, action, volume, price, status, execution_time_ms, created_at
    2. Create BFF route at ui-next/src/app/api/dashboard/executions/route.ts:
       - Proxy to backend /api/dashboard/executions
    3. Create ui-next/src/components/dashboard/recent-executions-widget.tsx:
       - Card with "Recent Executions" header and Activity icon
       - Table/list showing last 10 executions:
         - Time (relative: "2m ago")
         - Symbol
         - Action (BUY/SELL with color coding)
         - Volume
         - Status badge (success=green, failed=red)
       - "View All" link to /dashboard/trades
       - Empty state: "No recent executions"
       - Loading skeleton
  </action>
  <verify>npm run build, widget shows execution history</verify>
  <done>Recent executions widget displays last 10 trades with status</done>
</task>

<task type="auto">
  <name>Task 2: Create equity chart widget</name>
  <files>ui-next/src/components/dashboard/equity-chart-widget.tsx, ui-next/src/app/api/dashboard/equity/route.ts, app/routers/dashboard.py</files>
  <action>
    1. Add backend endpoint in app/routers/dashboard.py:
       - GET /dashboard/equity?days=30 - Return equity history
       - Query AccountEquityHistory model (created in Phase 22)
       - Aggregate by day, return: date, total_equity (sum across accounts)
       - If no history, return empty array
    2. Create BFF route at ui-next/src/app/api/dashboard/equity/route.ts
    3. Create ui-next/src/components/dashboard/equity-chart-widget.tsx:
       - Card with "Account Equity" header and TrendingUp icon
       - Use recharts LineChart or AreaChart
       - X-axis: dates, Y-axis: equity value
       - Gradient fill under line (green if up, red if down)
       - Time range selector: 7d, 30d, 90d
       - Current total balance display above chart
       - Empty state: "No equity history yet"
       - Loading skeleton
    4. Install recharts if not already: npm install recharts
  </action>
  <verify>npm run build, equity chart renders with data or empty state</verify>
  <done>Equity chart shows balance history over selected period</done>
</task>

<task type="auto">
  <name>Task 3: Create trial status and open positions widgets</name>
  <files>ui-next/src/components/dashboard/trial-status-widget.tsx, ui-next/src/components/dashboard/open-positions-widget.tsx, ui-next/src/app/api/dashboard/positions/route.ts, ui-next/src/app/dashboard/page.tsx, app/routers/dashboard.py</files>
  <action>
    1. Create ui-next/src/components/dashboard/trial-status-widget.tsx:
       - Card with "Subscription Status" header
       - For now (no trial system yet), show current subscription tier:
         - Free tier: "Free Plan - Limited to 1 broker"
         - Pro tier: "Pro Plan - Unlimited brokers"
       - When trial system added (Phase 24), will show:
         - "X trades remaining" or "X days left"
         - Progress bar for trial usage
       - "Upgrade" button for free users linking to /dashboard/settings/billing
       - Use subscription_tier from user context
    2. Add backend endpoint in app/routers/dashboard.py:
       - GET /dashboard/positions - Return open positions across all accounts
       - Query Position model where is_active=True
       - Return: symbol, volume, type, unrealized_pnl, account_info
    3. Create BFF route at ui-next/src/app/api/dashboard/positions/route.ts
    4. Create ui-next/src/components/dashboard/open-positions-widget.tsx:
       - Card with "Open Positions" header and Briefcase icon
       - Table showing current open positions:
         - Symbol
         - Side (Long/Short with color)
         - Size
         - P&L (green/red based on value)
         - Account (broker name)
       - Total P&L summary at bottom
       - Empty state: "No open positions"
       - Loading skeleton
    5. Update ui-next/src/app/dashboard/page.tsx:
       - Import and add all 4 new widgets
       - Organize layout:
         - Row 1: Stats grid (existing)
         - Row 2: Equity chart (full width or 2/3) + Trial status (1/3)
         - Row 3: Broker connections (existing)
         - Row 4: Open Positions + Recent Executions (side by side)
         - Row 5: Risk Usage + Rejected Signals (existing)
         - Row 6: Recent Activity placeholder (existing, can be removed/replaced)
  </action>
  <verify>npm run build, all widgets render correctly on dashboard</verify>
  <done>Trial status shows subscription, open positions displays current positions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GET /api/dashboard/executions returns recent executions
- [ ] GET /api/dashboard/equity returns equity history
- [ ] GET /api/dashboard/positions returns open positions
- [ ] Recent executions widget shows last 10 trades
- [ ] Equity chart renders with time range selector
- [ ] Trial/subscription status widget shows current tier
- [ ] Open positions widget shows active positions
- [ ] Dashboard layout is organized and responsive
- [ ] All widgets have loading skeletons
- [ ] All widgets have empty states
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard provides comprehensive trading overview
- All widgets load data correctly
- Responsive layout works on all screen sizes
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-settings-dashboard/23-05-SUMMARY.md`
</output>
