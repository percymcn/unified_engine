---
phase: 23-user-settings-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/users.py
  - app/schemas/user.py
  - ui-next/src/app/dashboard/settings/profile/page.tsx
  - alembic/versions/014_add_user_profile_fields.py
autonomous: true

must_haves:
  truths:
    - "User can view their profile information"
    - "User can update their name and email"
    - "User can change their password with validation"
    - "Password change requires current password verification"
  artifacts:
    - path: "app/routers/users.py"
      provides: "Profile API endpoints"
      exports: ["router"]
      contains: "get_profile|update_profile|change_password"
    - path: "ui-next/src/app/dashboard/settings/profile/page.tsx"
      provides: "Profile settings UI"
      min_lines: 100
  key_links:
    - from: "ui-next/src/app/dashboard/settings/profile/page.tsx"
      to: "/api/users/me"
      via: "fetch for profile operations"
      pattern: "fetch.*users/me"
---

<objective>
Implement user profile management with name/email editing and password change functionality.

Purpose: Enable users to manage their profile information and update their password securely (SET-01, SET-02).
Output: Profile settings page with API endpoints for profile updates and password changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@app/models/models.py (User model with full_name, email, hashed_password)
@app/routers/auth.py (existing auth patterns)
@ui-next/src/app/dashboard/settings/accounts/page.tsx (settings page pattern)
@ui-next/src/components/sidebar.tsx (navigation structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile fields migration and schemas</name>
  <files>alembic/versions/014_add_user_profile_fields.py, app/schemas/user.py</files>
  <action>
    1. Create migration to add avatar_url column to users table (nullable String)
    2. Add Pydantic schemas in app/schemas/user.py:
       - ProfileResponse: id, email, username, full_name, avatar_url, created_at
       - ProfileUpdate: full_name (optional), email (optional), avatar_url (optional)
       - PasswordChange: current_password, new_password, confirm_password
       - PasswordChangeResponse: success, message
    3. Add validation: new_password != current_password, confirm matches new
  </action>
  <verify>alembic upgrade head succeeds, python -c "from app.schemas.user import ProfileResponse, ProfileUpdate, PasswordChange"</verify>
  <done>Migration applied, schemas importable</done>
</task>

<task type="auto">
  <name>Task 2: Create profile API endpoints</name>
  <files>app/routers/users.py, app/main.py</files>
  <action>
    1. Create app/routers/users.py with:
       - GET /users/me/profile - Return current user's profile (ProfileResponse)
       - PUT /users/me/profile - Update profile (ProfileUpdate) - partial updates only
       - POST /users/me/password - Change password (PasswordChange)
         - Verify current_password against hashed_password using pwd_context
         - Hash new_password and update user record
         - Return success message
    2. Use get_current_user dependency from auth.py
    3. Handle email uniqueness check on update
    4. Register router in main.py with prefix="/api/users"
  </action>
  <verify>curl -H "Authorization: Bearer $TOKEN" http://localhost:8765/api/users/me/profile returns profile data</verify>
  <done>All 3 endpoints return correct responses</done>
</task>

<task type="auto">
  <name>Task 3: Create profile settings UI page</name>
  <files>ui-next/src/app/dashboard/settings/profile/page.tsx, ui-next/src/components/sidebar.tsx</files>
  <action>
    1. Create profile settings page with:
       - Profile form: full_name input, email input (read-only or editable), avatar URL input
       - Form validation using react-hook-form pattern
       - Save button with loading state
       - Success/error toast notifications
    2. Add password change section below profile:
       - Current password (required)
       - New password with strength indicator (min 8 chars)
       - Confirm password with match validation
       - Change Password button with loading state
    3. Use Card components matching existing settings pages
    4. Add "Profile" link to sidebar in settingsNavigation (icon: UserCircle)
    5. Fetch current profile on mount, populate form
    6. Call BFF route /api/users/me/profile for GET/PUT
    7. Call BFF route /api/users/me/password for password change
  </action>
  <verify>npm run build succeeds, page renders at /dashboard/settings/profile</verify>
  <done>Profile page shows user data, updates save correctly, password change works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration 014 applies without errors
- [ ] GET /api/users/me/profile returns current user data
- [ ] PUT /api/users/me/profile updates user profile
- [ ] POST /api/users/me/password changes password (verify with re-login)
- [ ] Profile page renders and loads user data
- [ ] Form validation works (email format, password match)
- [ ] Sidebar shows Profile link
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- User can view and edit their profile
- Password change with validation works
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-settings-dashboard/23-01-SUMMARY.md`
</output>
