---
phase: 23-user-settings-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - ui-next/src/providers/theme-provider.tsx
  - ui-next/src/providers/user-provider.tsx
  - ui-next/src/app/layout.tsx
  - ui-next/src/app/dashboard/layout.tsx
  - ui-next/src/components/header.tsx
  - ui-next/src/components/theme-toggle.tsx
autonomous: true

must_haves:
  truths:
    - "User can toggle between dark and light mode"
    - "Theme preference persists across page refreshes"
    - "Dashboard header shows actual logged-in user's name/email"
    - "User avatar initials display correctly in header"
  artifacts:
    - path: "ui-next/src/providers/theme-provider.tsx"
      provides: "Theme context and persistence"
      min_lines: 20
    - path: "ui-next/src/providers/user-provider.tsx"
      provides: "User context with current user data"
      min_lines: 30
    - path: "ui-next/src/components/theme-toggle.tsx"
      provides: "Dark/light mode toggle button"
      min_lines: 15
  key_links:
    - from: "ui-next/src/components/header.tsx"
      to: "ui-next/src/providers/user-provider.tsx"
      via: "useUser hook"
      pattern: "useUser"
    - from: "ui-next/src/components/theme-toggle.tsx"
      to: "ui-next/src/providers/theme-provider.tsx"
      via: "useTheme hook"
      pattern: "useTheme"
---

<objective>
Implement dark/light mode theme toggle and user context provider to show actual user in header.

Purpose: Enable theme customization (SET-05) and fix header to show real user data (SET-06, SET-07).
Output: Theme provider with toggle, user context provider, and properly wired header.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/23-user-settings-dashboard/23-02-SUMMARY.md (preferences API created)

Relevant source files:
@ui-next/src/components/header.tsx (needs user prop wired)
@ui-next/src/components/user-nav.tsx (uses user prop)
@ui-next/src/app/dashboard/layout.tsx (wraps dashboard)
@ui-next/src/app/layout.tsx (root layout)
@ui-next/src/app/dashboard/settings/billing/page.tsx (Stripe portal pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement theme provider with next-themes</name>
  <files>ui-next/src/providers/theme-provider.tsx, ui-next/src/components/theme-toggle.tsx, ui-next/src/app/layout.tsx, ui-next/package.json</files>
  <action>
    1. Install next-themes: npm install next-themes
    2. Create ui-next/src/providers/theme-provider.tsx:
       - Export ThemeProvider wrapping next-themes ThemeProvider
       - Configure: attribute="class", defaultTheme="system", enableSystem=true
       - Export useTheme hook re-export from next-themes
    3. Create ui-next/src/components/theme-toggle.tsx:
       - Button that toggles between light/dark/system
       - Use Sun/Moon icons from lucide-react
       - Dropdown with: Light, Dark, System options
       - Use DropdownMenu from shadcn/ui
    4. Update ui-next/src/app/layout.tsx:
       - Wrap children with ThemeProvider
       - Add suppressHydrationWarning to html tag
    5. Ensure tailwind.config.js has darkMode: "class" (likely already set)
  </action>
  <verify>npm run build succeeds, theme toggle appears and switches themes</verify>
  <done>Theme toggle works, preference persists via localStorage</done>
</task>

<task type="auto">
  <name>Task 2: Create user context provider</name>
  <files>ui-next/src/providers/user-provider.tsx, ui-next/src/app/api/users/me/route.ts</files>
  <action>
    1. Create BFF route at ui-next/src/app/api/users/me/route.ts:
       - GET handler that fetches from backend /api/users/me/profile
       - Pass through Authorization cookie/header
       - Return user data or null if not authenticated
    2. Create ui-next/src/providers/user-provider.tsx:
       - UserContext with: user (User | null), loading (boolean), refetch function
       - UserProvider component that fetches user on mount
       - useUser hook that returns context
       - Handle auth errors gracefully (return null user, don't crash)
    3. User type: { id, email, username, full_name, avatar_url, timezone, subscription_tier }
    4. Cache user data, provide refetch for updates
  </action>
  <verify>npm run build succeeds, useUser hook returns current user data</verify>
  <done>User context provides actual user data throughout app</done>
</task>

<task type="auto">
  <name>Task 3: Wire user and theme to header</name>
  <files>ui-next/src/app/dashboard/layout.tsx, ui-next/src/components/header.tsx</files>
  <action>
    1. Update ui-next/src/app/dashboard/layout.tsx:
       - Wrap with UserProvider inside WebSocketProvider
       - Import UserProvider from providers/user-provider
    2. Update ui-next/src/components/header.tsx:
       - Import and use useUser hook
       - Pass user data to UserNav component
       - Add ThemeToggle component next to ConnectionStatusIndicator
       - Show loading skeleton while user loads
    3. UserNav will now receive actual user (not placeholder)
    4. Verify billing page /dashboard/settings/billing has working Stripe portal link
       - If not, add "Manage Billing" button that calls /api/billing/portal
       - Opens Stripe Customer Portal in new tab
  </action>
  <verify>npm run build, header shows actual logged-in user name/email, theme toggle visible</verify>
  <done>Header displays real user, theme toggle works, billing portal accessible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm install next-themes succeeds
- [ ] Theme toggle appears in header
- [ ] Clicking theme toggle changes between light/dark modes
- [ ] Theme persists after page refresh
- [ ] Header shows actual logged-in user's name
- [ ] UserNav dropdown shows correct email
- [ ] Avatar initials match user name
- [ ] Billing page has working "Manage Billing" button (Stripe portal)
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Theme toggle works with persistence
- Header shows real user data
- Stripe portal accessible from billing
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-settings-dashboard/23-03-SUMMARY.md`
</output>
