---
phase: 23-user-settings-dashboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-next/src/app/dashboard/page.tsx
  - ui-next/src/components/dashboard/dashboard-skeleton.tsx
  - ui-next/src/components/dashboard/test-webhook-button.tsx
  - ui-next/src/hooks/use-websocket.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard shows loading skeletons while data fetches"
    - "Dashboard stats update in real-time via WebSocket"
    - "Test Webhook button sends test signal and shows result"
    - "Connection status overview shows all brokers at glance"
  artifacts:
    - path: "ui-next/src/components/dashboard/dashboard-skeleton.tsx"
      provides: "Loading skeleton components"
      min_lines: 30
    - path: "ui-next/src/components/dashboard/test-webhook-button.tsx"
      provides: "Test webhook quick action"
      min_lines: 25
  key_links:
    - from: "ui-next/src/app/dashboard/page.tsx"
      to: "ui-next/src/providers/websocket-provider.tsx"
      via: "useWebSocket for real-time updates"
      pattern: "useWebSocket|onMessage"
---

<objective>
Enhance dashboard with loading states, real-time WebSocket updates, and test webhook functionality.

Purpose: Improve dashboard UX with graceful loading and real-time data (DASH-01, DASH-02, DASH-03, DASH-04).
Output: Dashboard with skeletons, WebSocket integration, enhanced broker overview, and test webhook button.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@ui-next/src/app/dashboard/page.tsx (current dashboard)
@ui-next/src/hooks/use-websocket.ts (WebSocket hook)
@ui-next/src/providers/websocket-provider.tsx (WebSocket context)
@ui-next/src/components/brokers/broker-health-grid.tsx (broker status)
@ui-next/src/components/ui/skeleton.tsx (skeleton component)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive dashboard loading skeletons</name>
  <files>ui-next/src/components/dashboard/dashboard-skeleton.tsx, ui-next/src/app/dashboard/page.tsx</files>
  <action>
    1. Create ui-next/src/components/dashboard/dashboard-skeleton.tsx:
       - StatCardSkeleton: Card with skeleton for title, value, description
       - StatsGridSkeleton: 4 StatCardSkeletons in grid layout
       - BrokerGridSkeleton: Grid of broker card skeletons
       - WidgetSkeleton: Generic card skeleton for dashboard widgets
       - DashboardSkeleton: Full page skeleton combining all above
    2. Update ui-next/src/app/dashboard/page.tsx:
       - Import skeleton components
       - Show StatsGridSkeleton while stats loading
       - Show BrokerGridSkeleton while brokers load
       - Show WidgetSkeleton for risk widgets while loading
       - Use Suspense boundaries where appropriate
    3. Ensure smooth transition from skeleton to content (no layout shift)
    4. Match skeleton sizes to actual content dimensions
  </action>
  <verify>npm run build succeeds, dashboard shows skeletons during load</verify>
  <done>All dashboard sections have proper loading skeletons</done>
</task>

<task type="auto">
  <name>Task 2: Wire WebSocket for real-time dashboard updates</name>
  <files>ui-next/src/app/dashboard/page.tsx, ui-next/src/hooks/use-websocket.ts</files>
  <action>
    1. Update dashboard page to subscribe to WebSocket messages:
       - Import useWebSocketContext from websocket-provider
       - Listen for message types: "stats_update", "trade_executed", "signal_received"
       - On stats_update: Update dashboard stats state
       - On trade_executed: Increment todaysTrades, show toast notification
       - On signal_received: Increment activeSignals, show toast
    2. Add visual indicator when real-time update received (subtle flash/pulse)
    3. Backend should already emit these events (verify in app/services/websocket_service.py)
    4. If backend doesn't emit stats_update, add it:
       - Create function to broadcast stats update after trade execution
       - Call from signal processor after successful execution
    5. Don't break existing WebSocket functionality
  </action>
  <verify>Execute a test signal, verify dashboard updates without refresh</verify>
  <done>Dashboard stats update in real-time when trades execute</done>
</task>

<task type="auto">
  <name>Task 3: Add test webhook button and enhance broker overview</name>
  <files>ui-next/src/components/dashboard/test-webhook-button.tsx, ui-next/src/app/dashboard/page.tsx, ui-next/src/app/api/webhooks/test/route.ts</files>
  <action>
    1. Create test webhook BFF route at ui-next/src/app/api/webhooks/test/route.ts:
       - POST handler that sends test signal to backend webhook endpoint
       - Use user's first webhook config or default test payload
       - Return { success: boolean, message: string, signal_id?: string }
    2. Create ui-next/src/components/dashboard/test-webhook-button.tsx:
       - Button with "Test Webhook" label and Zap icon
       - Loading spinner while test runs
       - Toast notification with result (success/failure)
       - Show signal ID on success for tracking
    3. Add TestWebhookButton to dashboard page:
       - Place in quick actions section above broker grid
       - Or as card with "Quick Actions" header
    4. Enhance BrokerHealthGrid display (DASH-03):
       - Verify all brokers show at glance
       - Add "total connected / total accounts" summary above grid
       - Ensure consistent card sizing
  </action>
  <verify>npm run build, click Test Webhook shows result, broker overview displays correctly</verify>
  <done>Test webhook button works, broker overview shows all connections at glance</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dashboard shows skeletons during initial load
- [ ] No layout shift when content loads
- [ ] WebSocket connection indicator shows "Connected"
- [ ] Dashboard updates when trade executes (if possible to test)
- [ ] Test Webhook button appears on dashboard
- [ ] Clicking Test Webhook sends signal and shows result
- [ ] Broker overview shows all connected accounts
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard has polished loading experience
- Real-time updates work via WebSocket
- Test webhook provides quick testing capability
- Broker overview is comprehensive
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-settings-dashboard/23-04-SUMMARY.md`
</output>
