---
phase: 23-user-settings-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/models.py
  - alembic/versions/015_add_user_preferences.py
  - app/routers/users.py
  - ui-next/src/app/dashboard/settings/preferences/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can select their timezone from dropdown"
    - "User can configure notification preferences"
    - "Timezone selection persists across sessions"
    - "Notification preferences are saved to database"
  artifacts:
    - path: "app/models/models.py"
      provides: "User model with timezone and notification_preferences"
      contains: "timezone|notification_preferences"
    - path: "ui-next/src/app/dashboard/settings/preferences/page.tsx"
      provides: "Preferences settings UI"
      min_lines: 100
  key_links:
    - from: "ui-next/src/app/dashboard/settings/preferences/page.tsx"
      to: "/api/users/me/preferences"
      via: "fetch for preferences"
      pattern: "fetch.*preferences"
---

<objective>
Implement user preferences for timezone selection and notification settings.

Purpose: Allow users to customize their experience with timezone and notification preferences (SET-03, SET-04).
Output: Preferences settings page with backend storage for user preferences.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@app/models/models.py (User model)
@ui-next/src/app/dashboard/settings/risk/page.tsx (settings page pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preferences columns to User model</name>
  <files>app/models/models.py, alembic/versions/015_add_user_preferences.py</files>
  <action>
    1. Add to User model in app/models/models.py:
       - timezone = Column(String(50), default="UTC")
       - notification_preferences = Column(JSON, default=lambda: {
           "trade_alerts": True,
           "error_notifications": True,
           "daily_summary": False,
           "email_notifications": True
         })
    2. Create migration 015_add_user_preferences.py:
       - Add timezone column with default "UTC"
       - Add notification_preferences JSON column with default dict
       - Make both columns nullable for existing users
    3. Import JSON from sqlalchemy in models.py if not already
  </action>
  <verify>alembic upgrade head, verify columns exist with: python -c "from app.models.models import User; print(User.timezone, User.notification_preferences)"</verify>
  <done>User model has timezone and notification_preferences columns</done>
</task>

<task type="auto">
  <name>Task 2: Create preferences API endpoints</name>
  <files>app/routers/users.py, app/schemas/user.py</files>
  <action>
    1. Add schemas to app/schemas/user.py:
       - NotificationPreferences: trade_alerts (bool), error_notifications (bool), daily_summary (bool), email_notifications (bool)
       - PreferencesResponse: timezone (str), notification_preferences (NotificationPreferences)
       - PreferencesUpdate: timezone (optional str), notification_preferences (optional NotificationPreferences)
    2. Add endpoints to app/routers/users.py:
       - GET /users/me/preferences - Return current preferences
       - PUT /users/me/preferences - Update preferences (partial update)
    3. Validate timezone against pytz.all_timezones list
    4. Return 400 if invalid timezone provided
  </action>
  <verify>curl -H "Authorization: Bearer $TOKEN" http://localhost:8765/api/users/me/preferences returns preferences</verify>
  <done>Preferences endpoints work correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create preferences settings UI page</name>
  <files>ui-next/src/app/dashboard/settings/preferences/page.tsx, ui-next/src/components/sidebar.tsx</files>
  <action>
    1. Create preferences settings page with:
       - Timezone section:
         - Searchable dropdown with common timezones (America/New_York, Europe/London, Asia/Tokyo, etc.)
         - Show current time in selected timezone as preview
       - Notification Preferences section:
         - Toggle switches for each preference:
           - Trade Alerts: "Receive notifications when trades execute"
           - Error Notifications: "Alert me about connection or execution errors"
           - Daily Summary: "Receive daily trading summary email"
           - Email Notifications: "Enable email notifications (master toggle)"
       - Save button with loading state
       - Success toast on save
    2. Add "Preferences" link to sidebar in settingsNavigation (icon: Settings2 or Sliders)
    3. Fetch preferences on mount from /api/users/me/preferences BFF route
    4. Create BFF route if needed at ui-next/src/app/api/users/me/preferences/route.ts
    5. Use Switch components from shadcn/ui for toggles
    6. Use Select/Combobox for timezone dropdown
  </action>
  <verify>npm run build succeeds, page renders at /dashboard/settings/preferences</verify>
  <done>Preferences page shows all settings, saves correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration 015 applies without errors
- [ ] GET /api/users/me/preferences returns current preferences
- [ ] PUT /api/users/me/preferences updates preferences
- [ ] Invalid timezone returns 400 error
- [ ] Preferences page renders with timezone dropdown
- [ ] Notification toggles work and save
- [ ] Sidebar shows Preferences link
- [ ] npm run build succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- User can select timezone from dropdown
- User can toggle notification preferences
- Settings persist correctly
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-settings-dashboard/23-02-SUMMARY.md`
</output>
