# Backend URL (server-side API calls)
BACKEND_URL=http://localhost:8765

# Backend URL (client-side, for WebSocket connections)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8765

# Public webhook URL (displayed to users for external integrations)
NEXT_PUBLIC_WEBHOOK_BASE_URL=https://api.tradeflow.fluxeo.net
